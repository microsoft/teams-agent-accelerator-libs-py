[project]
name = "evals"
version = "0.1.0"
description = "Benchmarks for the different components"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "memory-module",
    "pandas",
    "mlflow>=2.17.2",
    "tqdm>=4.66.5",
    "databricks-sdk",
]

[tool.uv]
package = false